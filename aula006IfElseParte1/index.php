<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>If/Else operadores de condicionais</title>
</head>
<body>
    <h1>If/Else operadores de condicionais</h1>

    <?php 

        // == -> Verifica se os valores são iguais
        // === -> Verifica se os valores e o tipo são iguais
        // != ou <> -> Verifica se os valores são diferentes
        // !== -> Verifica se os valores são diferentes e de tipos diferentes
        // < -> Verifica se o valor da esquerda é menor que o valor da direta
        // > -> Verifica se o valor da esquerda é maior que o valor da direta
        // <= -> Verifica se o valor da esquerda é menor ou igual que o valor da direta
        // >= -> Verifica se o valor da esquerda é maior ou igual que o valor da direta

        if(2 !== 2) {
            echo 'Verdadeiro';
        } else {
            echo 'Falso';
        }

    ?>
</body>
</html>